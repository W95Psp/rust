module Core.Core_arch.X86.Adx
#set-options "--fuel 0 --ifuel 1 --z3rlimit 15"
open Core

let v__addcarry_u32 (c_in: u8) (a b out: u32) : (u32 & u8) =
  let output:u8 = () in
  out, output

let v__addcarryx_u32 (c_in: u8) (a b out: u32) : (u32 & u8) =
  let output:u8 = () in
  out, output

let v__subborrow_u32 (c_in: u8) (a b out: u32) : (u32 & u8) =
  let output:u8 = () in
  out, output