module Core.Unicode.Printable
#set-options "--fuel 0 --ifuel 1 --z3rlimit 15"
open Core

let check (x: u16) (singletonuppers: slice (u8 & u8)) (singletonlowers normal: slice u8) : bool = ()

let is_printable (x: char) : bool = ()

let v_SINGLETONS0U: slice (u8 & u8) = ()

let v_SINGLETONS0L: slice u8 = ()

let v_SINGLETONS1U: slice (u8 & u8) = ()

let v_SINGLETONS1L: slice u8 = ()

let v_NORMAL0: slice u8 = ()

let v_NORMAL1: slice u8 = ()